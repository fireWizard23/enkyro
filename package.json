{
  "name": "valid-req",
  "version": "1.0.0",
  "description": "A simple but powerful way to validate request object in express",
  "main": "lib/index.js",
  "files": [
    "lib/"
  ],
  "types": "lib/",
  "scripts": {
    "test": "mocha --check-leaks --require ts-node/register test/**/*.spec.ts",
    "test:watch": "mocha --require ts-node/register --watch --watch-files src/**/*.ts, test/**/*.spec.ts",
    "test-single": "mocha --require ts-node/register",
    "test-server": "mocha --require ts-node/register test/validateRequestBody.spec.ts"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.2.22",
    "@types/express": "^4.17.13",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "chai-http": "^4.3.0",
    "express": "^4.17.1",
    "mocha": "^9.1.3",
    "ts-node": "^10.4.0",
    "typescript": "^4.4.4"
  }
}
